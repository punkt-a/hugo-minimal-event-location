<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>{{ .Site.Title }} {{ .Site.Params.Location }}s</title>
        <link>{{ .Permalink }}</link>
        <description>Feed of all {{ .Site.Title }} {{ .Site.Params.Location }}s</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>{{ .Site.LanguageCode }}</language>
        <lastBuildDate>{{ time.Format "Mon, 02 Jan 2006 15:04:05 -0700" now | safeHTML }}</lastBuildDate>
        {{- with .OutputFormats.Get "RSS" }}
        {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
        {{- end -}}
        {{- range .Pages.ByPublishDate.Reverse }}
        <item>
            <title>{{ .Title }}</title>
            <link>{{ .Permalink }}</link>
            <pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
            <guid>{{ .Permalink }}</guid>
            {{- $address := printf "%s, %s %s" .Params.Street .Params.Zipcode .Params.City}}
            <description>{{ printf "%s (%s), %s" .Title .Params.District $address }}</description>
            <district>{{ .Params.District }}</district>
            <address>{{ $address }}</address>
            <latitude>{{ .Params.Latitude }}</latitude>
            <longtitude>{{ .Params.Longitude }}</longtitude>
            {{- if isset .Site.Params "umap" }}
            <umapLink>{{ printf "%s?feature=%s (%s)" .Site.Params.Umap .Title .Params.District | safeURL }}</umapLink>
            {{- end }}
            <content type="html">{{ printf `<![CDATA[%s]]>` .Content | safeHTML }}</content>
        </item>
        {{- end }}
    </channel>
</rss>
